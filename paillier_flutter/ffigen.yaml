# Run with `dart run ffigen --config ffigen.yaml`.
name: PaillierFlutterBindings
description: |
  Bindings for `src/paillier_flutter.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/paillier_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/paillier_flutter.h'
  include-directives:
    - 'src/paillier_flutter.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
